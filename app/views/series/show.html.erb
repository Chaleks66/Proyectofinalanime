<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @serie.name %>
</p>

<p>
  <strong>Year:</strong>
  <%= @serie.year %>
</p>


<p>
  <strong>Status:</strong>
  <%= @serie.status %>
</p>

<p>
  <strong>Img:</strong>
  <%= @serie.img %>
</p>

<%= link_to 'Edit', edit_serie_path(@serie) %> |
<%= link_to 'Back', series_path %>


<p>
Comments: <% @serie.comments.each do |post| %>
  <div class="card" style="width: 18rem;">
  <%= image_tag  @serie.img, class: "card-img-top", style:'filter:grayscale(100%)' %>
    <div class="card-body">
      <h5 class="card-title"><%= serie.name %></h5>
      <h5 class="card-body"><%= post.content %></h5>
    </div>
  </div>
      <% end %>
</p>

<%= link_to "Add Comment", new_comment_path(serie_id: @serie.id) %>