<%= form_with(model: serie_comment, local: true) do |form| %>
  <% if serie_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(serie_comment.errors.count, "error") %> prohibited this serie_comment from being saved:</h2>

      <ul>
      <% serie_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="field">
    <%= form.label :series_id %>
    <%= form.text_field :series_id %>
  </div>

  <div class="field">
    <%= form.label :users_id %>
    <%= form.text_field :users_id %>
  </div>

  

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
